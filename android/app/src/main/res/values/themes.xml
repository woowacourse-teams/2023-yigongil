<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Team201" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/black01_0D1117</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">
            @android:color/transparent
        </item>
        <!-- statusBar -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- 바텀 시트 다이얼로그 스타일 적용 -->
        <item name="bottomSheetDialogTheme">@style/BottomSheetDialogTheme</item>

        <!-- Text, EditText 하이라이트 및 입력 커서 스타일 적용 -->
        <item name="android:textColorLink">@color/green02_E639D353</item>
        <item name="android:textColorHighlight">@color/green04_26A641</item>
        <item name="colorControlActivated">@color/green04_26A641</item>

        <!-- ActionBar Menu Text Color -->
        <item name="android:actionMenuTextColor">@color/white</item>
    </style>

    <style name="Theme.Team201" parent="Base.Theme.Team201" />

    <!-- 피커 폰트, 구분선, 텍스트 컬러, 사이즈 테마 -->
    <style name="Theme.Picker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/pretendard_semibold</item>
        <item name="android:colorControlNormal">@color/transparent</item>
        <item name="android:textSize">26dp</item>
        <item name="android:textColorPrimary">@color/white</item>
    </style>

    <!-- 바텀 시트 다이얼로그 탑 모서리 라운딩 테마  -->
    <style name="BottomSheetDialogTheme" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_bottom_sheet_top_radius_20dp</item>
    </style>

    <!-- 캘린더뷰 폰트, 컬러 테마 -->
    <style name="CalendarTextAppearance" parent="TextAppearance.MaterialCalendarWidget.Date">
        <item name="android:textAppearance">@style/text_sb16</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/mcv_text_date_dark</item>
    </style>

    <!-- 이미지뷰 스타일 -->
    <style name="circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="ThemeOverlay.Toolbar" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <!-- Text color -->
        <item name="android:editTextColor">@color/white</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@color/grey01_C5C5C5</item>

        <item name="android:colorControlNormal">@color/white</item>
    </style>

    <!-- No margin Dialog -->
    <style name="NoMarginDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:textColorLink">@color/green02_E639D353</item>
        <item name="android:textColorHighlight">@color/green04_26A641</item>
        <item name="colorControlActivated">@color/green04_26A641</item>
        <item name="android:windowBackground">@color/black01_0D1117</item>
        <item name="android:windowIsFloating">false</item>
    </style>

</resources>
